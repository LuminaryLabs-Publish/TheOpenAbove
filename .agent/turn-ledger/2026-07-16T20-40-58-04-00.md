# Turn Ledger: TheOpenAbove Camera Capture Zoom Projection

**Timestamp:** `2026-07-16T20-40-58-04-00`  
**Repository:** `LuminaryLabs-Publish/TheOpenAbove`  
**Branch:** `main`  
**Status:** `camera-capture-zoom-projection-authority-audited`

## Summary

Selected TheOpenAbove by the oldest synchronized documented-selection rule after confirming all ten eligible Publish repositories were tracked, had root `.agent` state and matched their central ledger heads.

The audit traced wheel input from the renderer canvas and global window through Image Capture, Experience and the Balloon Camera Rig. It found two simultaneous wheel owners and a projection/scoring mismatch: capture zoom changes a private scalar and camera FOV, the flight camera also changes follow distance, and the next camera-rig update overwrites FOV before capture evaluation and rendering.

## Work completed

- Compared the 11-repository Publish inventory with ten eligible central ledgers.
- Excluded `LuminaryLabs-Publish/TheCavalryOfRome`.
- Selected only `LuminaryLabs-Publish/TheOpenAbove`.
- Re-read the semantic interaction loop, all domains, all 121 active surfaces and offered services.
- Inspected `image-capture-domain.js`, `experience-domain.js`, `balloon-camera-rig-kit.js`, `meadow-lift-scene.js`, `journey-domain.js` and the map overlay.
- Defined `open-above-sightseeing-camera-zoom-projection-authority-domain`.
- Added architecture, render, gameplay, interaction, camera-zoom, deployment and central-sync audits.
- Refreshed root `.agent` indexes and machine registry.

## Source-backed result

```txt
renderer-canvas wheel listener: present
global wheel listener: present
capture private zoom scalar: present
camera-rig follow-distance zoom: present
capture FOV write: present
camera-rig FOV overwrite next frame: present
score reads committed projection: no
single active zoom owner: no
CameraProjectionResult: no
FirstZoomBoundFrameAck: no
```

## Runtime boundary

No runtime source, tests, package scripts, workflow or deployment behavior was changed. No browser defect was reproduced and no production-readiness claim is made.