# Turn Ledger: TheOpenAbove Terrain LOD Transition Audit

**Timestamp:** `2026-07-11T16-30-25-04-00`

## Goal

Document the mismatch between current camera-relative horizon LOD policy and the geometry retained in the active horizon chunk map, then define the authority and fixture boundary needed for deterministic, budgeted and atomic LOD transitions.

## Completed

- Compared the ten accessible `LuminaryLabs-Publish` repositories with the nine eligible central ledger records.
- Excluded `TheCavalryOfRome`.
- Selected only `TheOpenAbove` as the oldest eligible centrally tracked repository.
- Read the root `.agent` state and prior terrain audits.
- Read the near terrain, horizon terrain, terrain surface and visual-domain sources.
- Confirmed the near streamer stores and rechecks LOD while the horizon streamer stores only coordinates.
- Confirmed retained horizon keys bypass geometry reclassification when camera-relative distance bands change.
- Identified the interaction loop, domains, 58 source-backed kits, 12 implied adapters and offered services.
- Added architecture, render, interaction, terrain-system, performance and deploy audits.
- Refreshed the required root `.agent` documents.
- Changed no runtime or deployment behavior.
- Used only `main`; created no branch or pull request.

## Main source finding

```txt
horizon create:
  distance -> segmentsForDistance -> geometry

horizon update after center movement:
  derive required key set
  if key already exists -> continue
  no intended segment classification
  no actual segment metadata
  no rebuild on LOD-band change
```

A chunk may therefore retain 4 segments after entering the `< 3400 m` band that declares 10 segments, or retain 10 segments after moving into a farther band.

## Required authority

```txt
open-above-terrain-lod-transition-authority-domain
  -> chunk identity and terrain-source revision
  -> current LOD classification
  -> transition plan and admitted build budget
  -> geometry build result
  -> edge/stitch policy
  -> atomic replacement
  -> intended/actual observation
  -> bounded journal and fixtures
```

## Validation

```txt
runtime changes: none
commands executed: none
browser/GPU smoke: not run
terrain transition fixtures: unavailable
central ledger synchronization: completed by this documentation run
```

No terrain LOD correctness or transition-performance claim is made.