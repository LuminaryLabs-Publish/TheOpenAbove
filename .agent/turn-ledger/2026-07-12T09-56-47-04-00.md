# Turn Ledger: 2026-07-12T09-56-47-04-00

## Intent

Replace the grain-like uniform meadow with a fixed-budget, dense textured grass field whose seeded world distribution contains lush patches, transitions and nearly bare openings.

## Plan ledger

**Goal:** render substantially denser-looking grass with fewer cards and stable procedural placement.

- [x] Replace three-plane single-blade patches with two crossed cards per clump.
- [x] Set the high-tier active budget to exactly 2,500 clumps and 5,000 cards.
- [x] Generate eight alpha-atlas variants containing 20–50 blades per card.
- [x] Add seeded macro, patch and detail noise with aggressive density remapping.
- [x] Generate 20–35 dense centers and 15–25 clearing centers with soft edges.
- [x] Use deterministic jittered-grid candidates and density acceptance.
- [x] Scale, rotate, lean, tint and texture-select every accepted clump.
- [x] Align clumps to sampled terrain normals.
- [x] Reject water, roads, steep slopes, the central exclusion and tree trunks.
- [x] Increase density around tree-cluster edges and low terrain while reducing exposed hilltops.
- [x] Retain instanced rendering, wind deformation, distance fading and chunk culling.
- [x] Add pure placement, budget, determinism and patch-contrast tests.
- [x] Run `npm run check`.
- [x] Run `npm run build`.
- [ ] Local browser preview was not admitted by the browser security boundary.
- [ ] Public Pages verification must follow the pushed deployment.

## Render contract

```txt
high active clumps: 2500
cards per clump: 2
high active cards: 5000
texture variants: 8
blades per card: 20–50
apparent blade range: 100,000–250,000
near geometry: crossed cards
placement: seeded and deterministic
```

Medium and low tiers preserve the same clump model with proportionally smaller instance budgets.

## Validation

```txt
npm run check: passed
npm run build: passed
campaign active-chunk generation: 2500 clumps / 5000 cards
short or underfilled campaign chunks: 0
density contrast fixture: passed
nearly bare sample fixture: passed
lush sample fixture: passed
repeat-generation parity: passed
Vite transformed modules: 68
```

No objective, region, progression rule, input binding, HUD element or flight simulation behavior changed.
