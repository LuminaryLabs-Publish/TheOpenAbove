# Turn Ledger: TheOpenAbove Map-Open Dual-Surface Render Work Budget

**Timestamp:** `2026-07-18T12-38-04-04-00`  
**Repository:** `LuminaryLabs-Publish/TheOpenAbove`  
**Branch:** `main`  
**Reviewed pre-audit head:** `28bed180bac93a326dfa1a31ab54699387698086`  
**Status:** `map-open-dual-surface-render-work-budget-authority-audited`

## Summary

TheOpenAbove was selected as the oldest synchronized eligible Publish repository after the complete 11-repository inventory was compared with central ledger and root `.agent` coverage. Cavalry of Rome was excluded.

The focused audit found two independently scheduled render loops while the parchment map is open: Journey continues the primary Experience render path with `dt: 0`, and the map overlay runs its own Canvas2D RAF. The map redraws unchanged decoration and reference-card construction every map frame without one shared surface plan or dirty-state budget.

## Completed

- compared all 11 Publish repositories;
- confirmed ten eligible central ledgers and root `.agent` states;
- confirmed zero new, ledger-missing, root-agent-missing, undocumented, or runtime-ahead eligible repositories;
- selected only TheOpenAbove by the oldest documented-selection rule;
- documented the complete interaction loop and domains;
- preserved the complete 125-surface kit/provider/adapter service inventory;
- inspected Journey, Meadow Lift, Navigation, parchment map rendering, overlay CSS, resize and retirement;
- defined the proposed 20-surface map-open dual-surface render authority;
- added architecture, render, gameplay, interaction, map-system, deploy and central-sync audits;
- refreshed the required root `.agent` documents and kit registry.

## Source-backed finding

```txt
Journey primary RAF remains active while map open: yes
Journey simulation update while map open: suppressed
Experience.render while map open: still called with dt zero
map overlay independent RAF while open: yes
scheduled render loops while map open: 2
fixed decorative-circle iterations per map draw: 90
reference gradient creations per map draw: 1
shared surface admission result: absent
map dirty-state redraw result: absent
map render work budget: absent
matching map frame acknowledgement: absent
```

At a hypothetical 60 displayed frames per second, source arithmetic yields 120 RAF callbacks, 60 main-scene render calls, 60 map draws, 5,400 decorative-circle iterations and 60 gradient creations per second. This is not measured browser or GPU evidence.

## Proposed authority

`open-above-map-open-dual-surface-render-work-budget-authority-domain`

## Validation boundary

Documentation only. Runtime, rendering, gameplay, input, tests, package scripts, workflows and deployment were not changed. Browser, artifact and Pages fixtures were not run.