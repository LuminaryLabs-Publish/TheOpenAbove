# Turn Ledger: HDR Attachment and Resolution Authority

**Timestamp:** `2026-07-12T05:11:46-04:00`  
**Repository:** `LuminaryLabs-Publish/TheOpenAbove`  
**Branch:** `main`

## Summary

Selected `TheOpenAbove` after the full Publish-to-ledger comparison. `PrehistoricRush` had newer repo-local documentation commits than its central ledger, so it was skipped to avoid overlapping active work.

The audit traced renderer/composer sizing and found two linked defects: the initial HDR depth texture is replaced without explicit ownership tracking, and browser resize manually rewrites replacement depth textures to CSS dimensions after the composer has been sized using the effective pixel ratio.

## Plan ledger

**Goal:** record a source-backed HDR attachment transaction and the exact proof required before renderer-resize correctness can be claimed.

- [x] Full organization comparison completed.
- [x] One repository selected.
- [x] `TheCavalryOfRome` excluded.
- [x] Interaction loop identified.
- [x] Domains identified.
- [x] All kits and services preserved.
- [x] Required root `.agent` files refreshed.
- [x] Timestamped audit family added.
- [x] Central ledger and internal change log synchronized.
- [x] Direct `main` writes only.
- [ ] Runtime implementation and executable fixtures remain open.

## Source files reviewed

```txt
AGENTS.md
package.json
src/main.js
src/visual/visual-domain.js
src/visual/quality-tier-kit.js
src/visual/post-process/hdr-composer-kit.js
tests/smoke.mjs
.agent/START_HERE.md
.agent/current-audit.md
.agent/next-steps.md
.agent/known-gaps.md
.agent/validation.md
.agent/kit-registry.json
```

## Finding

```txt
resolution.resize:
  composer.setPixelRatio(effectivePixelRatio)
  composer.setSize(cssWidth, cssHeight)

visual.resize:
  resolution.resize(cssWidth, cssHeight)
  hdrComposer.resize(cssWidth, cssHeight)

hdrComposer.resize:
  composer.setSize(cssWidth, cssHeight)
  depthTexture.image.width = cssWidth
  depthTexture.image.height = cssHeight
```

The color target's physical dimensions are determined by the composer's pixel ratio, while the manual depth assignments use unscaled CSS dimensions. Dynamic-scale transitions take only the first path, so source parity is also missing.

## Output

Current audit status:

```txt
hdr-attachment-resolution-lifecycle-authority-audited
```

Next implementation ledge:

```txt
HDR Attachment and Resolution Authority
+ Unified Resize Transaction
+ Attachment Ownership and Retirement
+ Framebuffer Completeness and Rollback
+ Visible-Frame Surface Receipt
```
