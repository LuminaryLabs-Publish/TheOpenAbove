# Turn Ledger: TheOpenAbove

**Timestamp:** `2026-07-10T21-31-01-04-00`

## Goal

Refresh the repo-local breakdown and central tracking state around active runtime authority without changing gameplay or rendering.

## Completed

- Compared the full accessible Publish inventory with central ledgers.
- Excluded `TheCavalryOfRome`.
- Selected `TheOpenAbove` as the oldest eligible documented repository.
- Read active runtime composition and lifecycle-relevant source.
- Cataloged interaction loop, domains, kits and services.
- Identified the import-time compatibility animation loop in `hot-air-balloon-object-kit.js`.
- Added timestamped architecture, render, gameplay, interaction, lifecycle and deploy audits.
- Refreshed root `.agent` routing files.
- Kept runtime code unchanged.
- Pushed directly to `main`; no branch or pull request was created.

## Finding

The reusable balloon object kit is not import-pure. Importing it starts `attachWhenReady`, which later starts a perpetual compatibility RAF even when the active route has no legacy vehicle to install into. The route's real RAF and all listener/resource disposers remain uncoordinated by a root session owner.

## Next order

1. Immutable runtime admission.
2. Import-pure balloon object kit and explicit compatibility installer.
3. Root session ownership, ordered teardown and reboot proof.