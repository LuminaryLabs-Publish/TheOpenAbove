# Turn Ledger: 2026-07-13T09-40-27-04-00

## Summary

Selected `LuminaryLabs-Publish/TheOpenAbove` after comparing the complete Publish inventory with central tracking. No eligible repository was new, ledger-missing or root-agent-missing, so the oldest documented repository rule applied.

## Plan ledger

**Goal:** document the missing coherence boundary between the continuously rendered Three.js world and the separately scheduled parchment-map canvas.

- [x] Inspect current central ledger and repo-local audit state.
- [x] Inspect `src/main.js`, `src/ui/parchment-map-overlay.js`, `src/visual/visual-domain.js` and `package.json`.
- [x] Trace both RAF chains and map open/close transitions.
- [x] Preserve the full domain, kit and service inventory.
- [x] Add a new tracker and audit family.
- [x] Refresh required root `.agent` documents.
- [x] Modify only this Publish repository.
- [x] Push directly to `main`.
- [x] Create no branch or pull request.
- [ ] Runtime implementation and executable proof remain open.

## Finding

The map overlay becomes visible synchronously, but its first matching draw is deferred to its own RAF callback. The WebGL world continues rendering through a separate RAF chain. Both surfaces read mutable state without a shared immutable envelope, projection result, commit result or visible-frame acknowledgement.

## Required authority

`open-above-map-world-dual-surface-frame-coherence-authority-domain`

## Validation

Documentation-only. Runtime, rendering, gameplay, dependencies, scripts and deployment were unchanged.