# Turn Ledger: TheOpenAbove Page-Lifecycle Suspension and Resume

**Timestamp:** `2026-07-16T07-58-10-04-00`  
**Repository:** `LuminaryLabs-Publish/TheOpenAbove`  
**Branch:** `main`  
**Status:** `page-lifecycle-flight-suspension-resume-authority-audited`

## Summary

Selected TheOpenAbove by the oldest synchronized ledger timestamp after confirming the 11-repository Publish inventory, Cavalry exclusion, ten eligible ledgers and root `.agent` states. The documentation pass identifies the missing document-lifecycle suspend/resume transaction around flight, Air Mail, world work, input, map animation and rendering.

## Plan ledger

**Goal:** preserve a durable record of repository selection, inspected sources, findings, output and validation boundaries.

- [x] Enumerate the Publish organization repository list.
- [x] Compare eligible repositories with `LuminaryLabs-Dev/LuminaryLabs` ledgers.
- [x] Exclude `TheCavalryOfRome`.
- [x] Select only TheOpenAbove.
- [x] Inspect host, simulation input, map scheduler, package and prior audit state.
- [x] Identify the interaction loop, domains, 101 active surfaces and services.
- [x] Add the timestamped lifecycle audit family.
- [x] Refresh required root `.agent` documents and machine registry.
- [ ] Implement or execute runtime lifecycle fixtures.

## Source observations

```txt
src/main.js
  flight RAF recursively schedules itself
  frameMs is clamped to 80 ms
  dt is clamped to 1/30 s
  no document lifecycle observer is installed

src/runtime/balloon-simulation-kit.js
  keys are held in a Set
  blur clears the Set
  visibility/page lifecycle does not produce a typed cancellation result

src/ui/parchment-map-overlay.js
  map-open state owns a second RAF
  only map open/close and dispose cancel that RAF
  no shared document lifecycle generation exists

package.json
  checks are static/headless and do not exercise browser visibility or BFCache behavior
```

## Finding

Browser backgrounding can suspend scheduling implicitly, but product state has no accepted suspension reason or generation. Resume clips elapsed wall time and continues, with no owned clock rebase, BFCache restoration result, stale-callback rejection or `FirstResumedFrameAck`.

## Required authority

`open-above-page-lifecycle-flight-suspension-resume-authority-domain`

## Change scope

```txt
documentation only
runtime unchanged
one repository selected
main branch only
no branch created
no pull request created
```

## Central record required

```txt
repo-ledger/LuminaryLabs-Publish/TheOpenAbove.md
internal-change-log/2026-07-16T07-58-10-04-00-the-open-above-page-lifecycle-suspension-resume.md
```